On-chain PositionManager reads for demo tokenIds 1000001–1000003 revert (NFTs do not exist).
`handlerInTee` then uses `config.demoPositions` (`dataSource=config`).
Simulate from `cre-workflow/`: `cre workflow simulate credit-scoring-workflow --target staging-settings --non-interactive --trigger-index 0 --http-payload ../docs/cre-simulation/payload-wallet-a.json`
Wallet A/B/C LTV: 7000 / 5500 / 2000.
